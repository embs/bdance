<% if user_signed_in? %>
  <li>
  <%= link_to(t('action.edit_registration'), edit_user_registration_path) %>
  </li>
<% else %>
  <li>
  <%= link_to(t('action.signup'), new_user_registration_path)  %>
  </li>
<% end %>
